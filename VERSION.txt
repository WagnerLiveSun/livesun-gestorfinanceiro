VERSÃO: 1.1.0
DATA: 2026-03-10
HORA: 00:00:00

CHANGELOG v1.1.0 (2026-03-10)
==============================

✅ SEGURANÇA / ISOLAMENTO MULTI-TENANT:
- Todas as queries de relatorios.py, dashboard.py, lancamentos.py, entidades.py, fluxo.py, contas_banco.py filtradas por empresa_id
- Novo helper src/tenant.py (scoped_query, scoped_get_or_404)
- Script de migração migrate_tenant_isolation.py: índices de empresa_id em 8 tabelas
- Script SQL migrations/002_fix_cross_tenant_fluxo_reference.sql para diagnóstico de referências cross-tenant

✅ AUTENTICAÇÃO POR EMPRESA:
- Login alterado para CPF/CNPJ da empresa + username + senha
- Username único por empresa (UniqueConstraint empresa_id+username); email globalmente único
- Helper _normalize_document() para aceitar CPF/CNPJ formatado ou só dígitos
- Campo CPF/CNPJ adicionado ao formulário de login
- Hints de escopo de username em register.html e add_user.html
- Empresa Padrão com CNPJ 00000000000000 criada automaticamente no boot
- Script de migração migrate_user_login_scope.py para schema legado

✅ CORREÇÕES DE DEPLOY:
- Hotfix: _create_default_user() com verificação backward-compatible para schema legado
- Catch de IntegrityError no startup para evitar crash quando índice global de username ainda existe

✅ TESTES:
- test_tenant_isolation.py atualizado: mesmo username em empresas diferentes permitido
- 6 testes passando (incluindo test_same_username_allowed_in_different_companies e test_login_uses_company_scope)

CHANGELOG v1.0.0 (2026-03-07)
==============================

✅ CORREÇÕES CRÍTICAS:
- Fix: Erro de sintaxe Jinja2 no template entidades/form.html (faltava {% endblock %})
- Fix: Proteção completa de acesso a empresa_id e vendedores em todas as rotas de entidades
- Fix: Logging detalhado com traceback completo para diagnóstico de erros
- Fix: Tratamento de erros robusto nas funções criar() e editar() de entidades
- Fix: Proteção de template contra valores nulos em current_user

✅ MELHORIAS DE DEPLOYMENT:
- Fix: Expansão correta da variável PORT no Railway (${PORT:-5000})
- Fix: Healthcheck ajustado para /auth/login
- Implementação de schema auto-migration no startup da aplicação
- Docker multi-stage build otimizado

✅ NOVAS FUNCIONALIDADES:
- Menu de Comissões adicionado ao sidebar (4 opções: Lista, Apuração, Relatório, Parâmetros)
- Campos valor_imposto e valor_outros_custos no formulário de lançamentos
- Sistema de compatibilidade de schema automático

✅ INFRAESTRUTURA:
- Deployment automático no Railway via Git push
- Scripts de deployment para Hostinger (FTP + SQL)
- Docker Compose para desenvolvimento local
- CI/CD pipeline com GitHub Actions

📦 COMPONENTES:
- Flask 3.0.0 + SQLAlchemy 2.0.23
- MySQL 8.0 (Railway + Hostinger)
- Gunicorn WSGI (4 workers, 120s timeout)
- Python 3.12-slim

🌐 AMBIENTES:
- Produção: https://appfinanceiro.livesun.live (Railway)
- Database: MySQL Railway + Hostinger fallback

✅ STATUS: PRODUÇÃO ESTÁVEL

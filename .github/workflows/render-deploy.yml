name: Deploy to Render

on:
  push:
    branches:
      - main  # Altere para 'master' se necessário

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy Hook
        run: |
          curl -X GET "https://api.render.com/deploy/srv-d6earcktgctc73durllg?key=PZK1Sbtqg3o"
